(config 
    :template               ;; Choose a Template and deactive the Manual Settings 
        ; 'doom             ;; - A mix between Modus and Doom
        'tsoding          ;; - Ever watched Tsoding?
    :ui                     ;; UI Tweaks
        'whitespace         ;; - Show the Whitespaces 
        'cleanUI            ;; - Make Emacs more Modern
    :utils                  ;; Activate your Language Server
        'multiple-cursors   ;; - One Cursor only?
        'magit              ;; - Git, almighty Git.
        'simpleclip         ;; - Who needs to copy if you can copy and paste
        'move-text          ;; - We are moooving
        'which-key          ;; - What was the Keybind again?
        'mac                ;; - Windows what?
    :cmp                    ;; Everybody needs a proper Completion 
        'vertico            ;; -
        'orderless          ;; -
        'marginalia         ;; -
        'counsel            ;; -
    :theme                  ;; Choose a Theme
        ; 'dark               ;; -
        'light            ;; -
)

